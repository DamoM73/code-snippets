# File Handling

## Check file exists

You can use two methods from the `os` module to check if a file exists

- `exists()`
- `isfile()`

### `exists()`

The `exists()` method will let you know if either a file or a directory exists.

```{literalinclude} ./python_files/file_exists.py
:linenos:
```